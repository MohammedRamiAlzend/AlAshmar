// Note: Handler interfaces have been replaced by MediatR's IRequestHandler.
// Commands and Queries now implement IRequest<TResult> via ICommand<T> and IQuery<T>.
// Use ISender.Send() in controllers instead of injecting individual handlers.
